

# Added by skill
TELEGRAM_BOT_TOKEN=
TELEGRAM_ONLY=

# Inject endpoint — exposes POST /inject for external triggers (iPhone Shortcuts, etc.)
# Set INJECT_HOST to your Tailscale IP (run: tailscale ip -4) so only
# Tailscale peers can reach it. Leave unset to disable the endpoint.
INJECT_SECRET=
INJECT_HOST=127.0.0.1
INJECT_PORT=3721
